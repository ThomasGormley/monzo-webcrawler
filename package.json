{
  "name": "monzo-webcrawler",
  "module": "src/cli.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --sourcemap ./src/cli.ts --outfile ./bin/crawl",
    "dev": "bun build --compile ./src/cli.ts --outfile ./bin/crawl-dev --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cheerio": "^0.22.35"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
